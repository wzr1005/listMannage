
server:
  port: 8090
  tomcat:
    uri-encoding: utf-8

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/simple?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password:
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp

# ??????
  devtools:
    add-properties: false

mybatis:
  mapper-locations: classpath:mapper/*.xml
